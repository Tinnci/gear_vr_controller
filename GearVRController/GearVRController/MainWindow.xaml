<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="GearVRController.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GearVRController"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="GearVRController">

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="12">
            <TextBlock Text="Gear VR Controller" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock x:Name="StatusText" Text="{Binding StatusMessage}"/>
            
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button x:Name="ConnectButton" 
                        Content="连接设备" 
                        Click="ConnectButton_Click"/>
                <Button x:Name="DisconnectButton" 
                        Content="断开连接" 
                        Click="DisconnectButton_Click"
                        IsEnabled="{Binding IsConnected}"/>
                <Button x:Name="ToggleControlButton"
                        Content="{Binding IsControlEnabled, Converter={StaticResource BoolToControlStateConverter}}"
                        Click="ToggleControlButton_Click"/>
            </StackPanel>

            <StackPanel Spacing="8">
                <TextBlock Text="控制设置" Style="{StaticResource SubtitleTextBlockStyle}"/>
                
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <CheckBox Content="启用鼠标控制" 
                             IsChecked="{Binding IsMouseEnabled, Mode=TwoWay}"/>
                    <CheckBox Content="启用键盘控制" 
                             IsChecked="{Binding IsKeyboardEnabled, Mode=TwoWay}"/>
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="鼠标灵敏度"/>
                    <Slider Minimum="0.1" 
                            Maximum="2.0" 
                            Value="{Binding MouseSensitivity, Mode=TwoWay}"
                            Width="200"
                            HorizontalAlignment="Left"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Spacing="12">
                    <Button Content="重置设置" 
                            Click="ResetSettingsButton_Click"/>
                    <Button Content="校准触摸板"
                            Click="CalibrateButton_Click"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,24,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Spacing="8">
                <TextBlock Text="按钮状态" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <CheckBox Content="扳机键" 
                          IsChecked="{Binding LastControllerData.TriggerButton}"
                          IsEnabled="False"/>
                <CheckBox Content="Home键" 
                          IsChecked="{Binding LastControllerData.HomeButton}"
                          IsEnabled="False"/>
                <CheckBox Content="返回键" 
                          IsChecked="{Binding LastControllerData.BackButton}"
                          IsEnabled="False"/>
                <CheckBox Content="触摸板" 
                          IsChecked="{Binding LastControllerData.TouchpadButton}"
                          IsEnabled="False"/>
                <CheckBox Content="音量+" 
                          IsChecked="{Binding LastControllerData.VolumeUpButton}"
                          IsEnabled="False"/>
                <CheckBox Content="音量-" 
                          IsChecked="{Binding LastControllerData.VolumeDownButton}"
                          IsEnabled="False"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Spacing="8">
                <TextBlock Text="传感器数据" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <TextBlock>
                    <Run Text="触摸板 X:"/>
                    <Run Text="{Binding LastControllerData.AxisX}"/>
                </TextBlock>
                <TextBlock>
                    <Run Text="触摸板 Y:"/>
                    <Run Text="{Binding LastControllerData.AxisY}"/>
                </TextBlock>
                <TextBlock>
                    <Run Text="加速度 X:"/>
                    <Run Text="{Binding LastControllerData.AccelX}"/>
                </TextBlock>
                <TextBlock>
                    <Run Text="加速度 Y:"/>
                    <Run Text="{Binding LastControllerData.AccelY}"/>
                </TextBlock>
                <TextBlock>
                    <Run Text="加速度 Z:"/>
                    <Run Text="{Binding LastControllerData.AccelZ}"/>
                </TextBlock>
                <TextBlock>
                    <Run Text="陀螺仪 X:"/>
                    <Run Text="{Binding LastControllerData.GyroX}"/>
                </TextBlock>
                <TextBlock>
                    <Run Text="陀螺仪 Y:"/>
                    <Run Text="{Binding LastControllerData.GyroY}"/>
                </TextBlock>
                <TextBlock>
                    <Run Text="陀螺仪 Z:"/>
                    <Run Text="{Binding LastControllerData.GyroZ}"/>
                </TextBlock>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
