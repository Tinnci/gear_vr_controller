<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="GearVRController.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GearVRController"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Title="GearVRController">

    <NavigationView x:Name="MainNavigationView"
        IsSettingsVisible="False"
        ExpandedModeThresholdWidth="1000"
        SelectionChanged="MainNavigationView_SelectionChanged">
        <NavigationView.Header>
            <Grid Padding="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="Gear VR Controller"
                    Style="{StaticResource TitleTextBlockStyle}"
                    VerticalAlignment="Center" />

                <StackPanel Grid.Column="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,0">
                    <Button x:Name="ConnectButton"
                        Content="连接设备"
                        Click="ConnectButton_Click"
                        Margin="0,0,8,0"
                        IsEnabled="{Binding IsConnecting, Converter={StaticResource BooleanNegationConverter}}" />
                    <Button x:Name="DisconnectButton"
                        Content="断开连接"
                        Click="DisconnectButton_Click"
                        IsEnabled="{Binding IsConnected}" />
                </StackPanel>
            </Grid>
        </NavigationView.Header>

        <NavigationView.MenuItems>
            <NavigationViewItem Tag="home" Icon="Home" Content="状态与控制" />
            <NavigationViewItem Tag="settings" Icon="Setting" Content="设置" />
            <NavigationViewItem Tag="calibrate" Icon="Sync" Content="校准" />
            <NavigationViewItem Tag="about" Icon="Help" Content="关于" />
        </NavigationView.MenuItems>

        <NavigationView.FooterMenuItems>
            <NavigationViewItem Tag="resetSettings" Icon="Refresh" Content="重置设置" />
            <NavigationViewItem Tag="visualizer" Icon="View" Content="触摸板可视化" />
        </NavigationView.FooterMenuItems>

        <Frame x:Name="ContentFrame" Margin="12,0,0,0" />
    </NavigationView>
</Window>