<Page
    x:Class="GearVRController.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GearVRController.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:viewmodels="using:GearVRController.ViewModels"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="12" Spacing="12">

            <!-- 鼠标设置 -->
            <Expander Header="鼠标设置" IsExpanded="True">
                <StackPanel Margin="0,12,0,0">
                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="鼠标灵敏度" />
                        <Slider Minimum="0.1"
                            Maximum="2.0"
                            Value="{Binding MouseSensitivity, Mode=TwoWay}"
                            Width="200"
                            HorizontalAlignment="Left"
                            IsEnabled="{Binding IsRelativeMode}" />
                    </StackPanel>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="平滑等级" />
                        <Slider Minimum="1"
                            Maximum="10"
                            Value="{Binding SmoothingLevel, Mode=TwoWay}"
                            Width="200"
                            HorizontalAlignment="Left"
                            IsEnabled="{Binding EnableSmoothing}"
                            Visibility="{Binding IsRelativeMode, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    </StackPanel>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="非线性曲线强度" />
                        <Slider Minimum="1.0"
                            Maximum="3.0"
                            Value="{Binding NonLinearCurvePower, Mode=TwoWay}"
                            Width="200"
                            HorizontalAlignment="Left"
                            IsEnabled="{Binding EnableNonLinearCurve}"
                            Visibility="{Binding IsRelativeMode, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    </StackPanel>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="死区大小" />
                        <Slider Minimum="0"
                            Maximum="20"
                            Value="{Binding DeadZone, Mode=TwoWay}"
                            Width="200"
                            HorizontalAlignment="Left"
                            Visibility="{Binding IsRelativeMode, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    </StackPanel>

                    <controls:WrapPanel HorizontalSpacing="12" VerticalSpacing="8">
                        <CheckBox Content="移动平滑"
                            IsChecked="{Binding EnableSmoothing, Mode=TwoWay}"
                            IsEnabled="{Binding IsRelativeMode}" />
                        <CheckBox Content="非线性曲线"
                            IsChecked="{Binding EnableNonLinearCurve, Mode=TwoWay}"
                            IsEnabled="{Binding IsRelativeMode}" />
                    </controls:WrapPanel>
                </StackPanel>
            </Expander>

            <!-- 手势设置 -->
            <Expander Header="手势设置" IsExpanded="True">
                <StackPanel Margin="0,12,0,0">
                    <ToggleSwitch Header="启用手势模式"
                        IsOn="{Binding IsGestureMode, Mode=TwoWay}"
                        OffContent="相对鼠标模式 (推荐)"
                        OnContent="手势模式" />

                    <StackPanel Margin="0,12,0,12">
                        <TextBlock Text="手势灵敏度" />
                        <Slider Minimum="0.1"
                            Maximum="1.0"
                            Value="{Binding GestureSensitivity, Mode=TwoWay}"
                            Width="200"
                            HorizontalAlignment="Left" />
                    </StackPanel>

                    <CheckBox Content="显示手势提示"
                        IsChecked="{Binding ShowGestureHints, Mode=TwoWay}"
                        Margin="0,0,0,12" />

                    <StackPanel Spacing="8">
                        <ComboBox Header="向上滑动动作"
                            ItemsSource="{Binding AvailableGestureActions}"
                            SelectedItem="{Binding SwipeUpAction, Mode=TwoWay}" />
                        <ComboBox Header="向下滑动动作"
                            ItemsSource="{Binding AvailableGestureActions}"
                            SelectedItem="{Binding SwipeDownAction, Mode=TwoWay}" />
                        <ComboBox Header="向左滑动动作"
                            ItemsSource="{Binding AvailableGestureActions}"
                            SelectedItem="{Binding SwipeLeftAction, Mode=TwoWay}" />
                        <ComboBox Header="向右滑动动作"
                            ItemsSource="{Binding AvailableGestureActions}"
                            SelectedItem="{Binding SwipeRightAction, Mode=TwoWay}" />
                    </StackPanel>
                </StackPanel>
            </Expander>

            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                <Button Content="重置设置"
                    Click="ResetSettingsButton_Click"
                    Margin="0,0,8,0" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page> 