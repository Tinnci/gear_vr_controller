<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="GearVRController.Views.TouchpadVisualizerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GearVRController.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="触摸板可视化">

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="触摸板可视化" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,12"/>

        <Grid Grid.Row="1" x:Name="TouchpadCanvas" Background="{ThemeResource SystemControlBackgroundAltMediumLowBrush}" 
              BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}" BorderThickness="1">
            <!-- 触摸板历史轨迹将在代码中动态绘制 -->
            <Canvas x:Name="HistoryCanvas"/>
            
            <!-- 当前触摸点 -->
            <Ellipse x:Name="TouchPoint" Width="20" Height="20" Fill="Red" Opacity="0.8" 
                     Canvas.ZIndex="10" Visibility="Collapsed"/>
            
            <!-- 坐标系中心标记 -->
            <Line x:Name="HorizontalLine" Stroke="{ThemeResource SystemControlForegroundBaseMediumBrush}" 
                  StrokeThickness="1" StrokeDashArray="4,2" Opacity="0.5"/>
            <Line x:Name="VerticalLine" Stroke="{ThemeResource SystemControlForegroundBaseMediumBrush}" 
                  StrokeThickness="1" StrokeDashArray="4,2" Opacity="0.5"/>
            
            <!-- 触摸板边界圆圈 -->
            <Ellipse x:Name="TouchpadBoundary" Stroke="{ThemeResource SystemControlForegroundBaseMediumBrush}" 
                     StrokeThickness="2" Fill="Transparent"/>
        </Grid>

        <StackPanel Grid.Row="2" Margin="0,12,0,0" Spacing="8">
            <TextBlock x:Name="TouchpadInfoText" Text="等待触摸板输入..." Style="{StaticResource BodyTextBlockStyle}"/>
            <TextBlock x:Name="GestureInfoText" Text="尚未检测到手势" Style="{StaticResource BodyTextBlockStyle}"/>
            
            <!-- 添加坐标和状态文本 -->
            <Grid Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Grid.Column="0" Text="X坐标: " Margin="0,0,4,0"/>
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="XValueText" Text="0.00"/>
                
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Y坐标: " Margin="0,0,4,0"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="YValueText" Text="0.00"/>
                
                <TextBlock Grid.Row="2" Grid.Column="0" Text="按下状态: " Margin="0,0,4,0"/>
                <TextBlock Grid.Row="2" Grid.Column="1" x:Name="PressedStateText" Text="未按下"/>
                
                <TextBlock Grid.Row="3" Grid.Column="0" Text="手势: " Margin="0,0,4,0"/>
                <TextBlock Grid.Row="3" Grid.Column="1" x:Name="GestureText" Text="无"/>
            </Grid>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8" Margin="0,8,0,0">
                <Button x:Name="ClearHistoryButton" Content="清除历史" Click="ClearHistoryButton_Click"/>
                <Button x:Name="CloseButton" Content="关闭" Click="CloseButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window> 