<Page
    x:Class="GearVRController.Views.CalibrationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GearVRController.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:GearVRController.ViewModels"
    mc:Ignorable="d">

    <StackPanel Padding="12">
        <TextBlock Text="触摸板校准" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,12" />
        <TextBlock Text="手动校准指南:" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,8" />
        <TextBlock
            Text="1. 点击 '开始校准' 按钮。
2. 等待触摸板校准窗口弹出。
3. 按照校准窗口的指示，在触摸板边缘划圈。
4. 校准完成后，窗口将自动关闭。"
            TextWrapping="Wrap" Margin="0,0,0,12" />

        <Button Content="开始校准" Click="StartCalibrationButton_Click"
            IsEnabled="{Binding IsConnected, Mode=OneWay}" Margin="0,0,0,12" />

        <TextBlock Text="校准状态:" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,8" />
        <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" Margin="0,0,0,12" />

        <TextBlock Text="上次校准数据:" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,8" />
        <StackPanel Margin="0,0,0,12">
            <TextBlock>
                <Run Text="MinX: " />
                <Run Text="{Binding CalibrationData.MinX, FallbackValue='N/A'}" />
            </TextBlock>
            <TextBlock>
                <Run Text="MaxX: " />
                <Run Text="{Binding CalibrationData.MaxX, FallbackValue='N/A'}" />
            </TextBlock>
            <TextBlock>
                <Run Text="MinY: " />
                <Run Text="{Binding CalibrationData.MinY, FallbackValue='N/A'}" />
            </TextBlock>
            <TextBlock>
                <Run Text="MaxY: " />
                <Run Text="{Binding CalibrationData.MaxY, FallbackValue='N/A'}" />
            </TextBlock>
        </StackPanel>
    </StackPanel>
</Page> 